# TimeSyncBot

Telegram бот для управления расписанием студентов ВятГУ. Помогает отслеживать пары, находить свободные аудитории и выявлять общее свободное время между группами.

## Возможности

- 📅 **Просмотр расписания** на день/неделю для вашей группы
- 🔎 **Поиск свободных аудиторий** в конкретное время
- 👥 **Поиск общего свободного времени** между несколькими группами
- ⭐ **Избранные группы** для быстрого доступа
- 🔄 **Автоматическое обновление** расписания по расписанию
- 📊 **Аналитика использования** бота

## Требования

- Python 3.11+
- Docker & Docker Compose (опционально)
- Telegram Bot Token (получить в [@BotFather](https://t.me/botfather))

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/smileyfaceskobochka/TimeSyncBot.git
cd TimeSyncBot
```

### 2. Создание виртуального окружения (опционально)

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Конфигурация

Создайте `.env` файл в корневой директории:

```env
BOT_TOKEN=ваш_токен_бота_здесь
ADMIN_IDS=123456789,987654321
TZ=Europe/Moscow
```

**Переменные окружения:**

- `BOT_TOKEN` - токен Telegram бота (обязательно)
- `ADMIN_IDS` - ID администраторов через запятую (обязательно)
- `TZ` - временная зона (по умолчанию: Europe/Moscow)
- `DATA_DIR` - директория для данных (по умолчанию: ./data)
- `DB_DIR` - директория для БД (по умолчанию: ./data)
- `LOG_DIR` - директория для логов (по умолчанию: ./logs)

## Использование

### Локальный запуск

```bash
python main.py
```

Бот начнёт работать и будет ждать входящих сообщений в Telegram.

### Docker запуск

```bash
docker-compose up -d
```

Проверить статус:

```bash
docker-compose logs -f timesyncbot
```

Остановить:

```bash
docker-compose down
```

## Команды пользователя

- `/start` - начать работу с ботом
- `/help` - справка о возможностях
- `/meet` - найти общее свободное время для групп
  - Пример: `/meet ИВТб ПИб`
  - Пример с датой: `/meet ИВТб ПИб 25.10`

## Архитектура

### Основные компоненты

1. **Parser** (`tgbot/services/parser/`)
   - Загружает расписание с сайта ВятГУ в PDF
   - Парсит PDF и извлекает информацию о парах
   - Сохраняет в SQLite базу

2. **Database** (`tgbot/database/`)
   - SQLModel ORM для работы с данными
   - Модели: User, TrackedGroup, Lesson, Schedule, ActionLog
   - Репозитории для асинхронного доступа

3. **Handlers** (`tgbot/handlers/`)
   - Обработка команд Telegram
   - FSM для управления состояниями диалога
   - Callback queries для кнопок

4. **Services** (`tgbot/services/`)
   - Логика поиска свободного времени
   - Проверка занятости аудиторий

### Поток данных

```txt
PDF сайта ВятГУ
    ↓
site_to_pdf.py (загрузка)
    ↓
pdf_parser.py (извлечение данных)
    ↓
occupancy_parser.py (анализ аудиторий)
    ↓
SQLite база (сохранение)
    ↓
Telegram UI (отображение)
```

## Админ команды

**Примечание:** Доступны только для пользователей в `ADMIN_IDS`

- `/health` - проверка статуса бота
- `/parse` - запустить парсинг расписания
- `/logs` - отправить файл логов

## Конфиг

### Time Slots (расписание пар)

В `tgbot/config.py` определены временные слоты:

```python
TIME_SLOTS = {
    1: ("08:30", "10:00"),
    2: ("10:10", "11:40"),
    # ... и т.д.
}
```
